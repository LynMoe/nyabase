# Worklog

- Session: 20260605-000113
- Lead as architect/doc-writer.
- Inspected current source tree, common enums/protocols, backend modules, and frontend container routes/components.
- Preparing repo-level design doc and harness design record.
- Wrote `docs/container-control-plane-redesign.md` as the repo-level implementation directive.
- Added `scripts/check-control-plane-redesign-conformance.sh` and package script `check:control-plane-redesign`.
- Ran conformance script; it fails as expected before implementation, listing old routes/commands and missing V2 services.
- Added harness `tests.md` summarizing required regression gates.
- Implemented first incompatible cutover step:
  - common `ContainerPhase` and V2 runtime container command names;
  - backend `v2/containers` controller route surface;
  - V2 skeleton services for action policy/control/operation/runtime observation/orphan;
  - frontend route replacement from serverId+containerId to containerId;
  - frontend operation tracker hook.
- Rebuilt common dist after contract changes.
- Verification after first cutover:
  - `pnpm typecheck`: passed.
  - `pnpm --filter @nyabase/common test`: passed 47 tests.
  - `pnpm --filter @nyabase/backend test -- operations.service.test.ts container-operations-dispatch.test.ts`: passed 14 tests.
  - `pnpm --filter @nyabase/agent test -- dispatcher.test.ts`: passed 31 tests.
  - common artifact guard: no output.
- Tightened conformance script to also reject hidden old services/direct action methods/list-visibility helpers.
- Current stricter conformance failures: old read-model/mounts/ssh-sync files still exist; old direct action methods still exist; live specs still use deleteContainer/waitForContainerByName helpers. These are the next required removals before completion.
- Continued incompatible V2 cutover:
  - tightened conformance gate to reject old REST container paths in tests, old Docker-ID public identity in tests, and frontend lifecycle docker binding inference;
  - rewrote frontend container list/detail/action flow to consume `ContainerView` and backend `actions` instead of old `ContainerDto`/dockerId/lifecycle inference;
  - switched create/list/mount frontend calls to `/v2/containers` and operation action endpoints;
  - added V2 container DB entities and registered them: desired specs, lifecycle, runtime containers/stats/GPU inventory, GPU allocations, runtime orphans;
  - added control-plane migration for new tables with DB reset semantics;
  - implemented V2 `ContainerControlService`/`ContainerOperationService` minimal durable operation enqueue path;
  - removed old operation/reconcile/runtime-observation tests that asserted legacy dockerId chain and replaced focused V2 operation/action-policy tests;
  - rewrote live spec references away from old `/containers` route strings and `spec.dockerId` identity reads to canonical container IDs.
- Verification after this pass:
  - `pnpm typecheck`: passed.
  - `pnpm test:unit`: passed (common 47, backend 105, agent 71 tests).
  - `pnpm test:functional`: passed shared-instance functional smoke (25 checks).
  - `bash scripts/check.sh`: passed; lint emitted warnings only, no errors.
  - `pnpm run check:control-plane-redesign`: passed with tightened checks.
  - common source artifact guard: no output.
- Remaining before goal completion: run/repair full live suite reset/register/deploy/smoke/admin-setup/personas/continuation and complete runtime observation/orphan/agent exec/stats production paths beyond the minimal durable enqueue implementation.
- Lead stopped direct patch-style architecture edits per user instruction.
- Persisted developer dispatch in `developer-dispatch.md`; implementation is delegated to a developer subagent with `docs/container-control-plane-redesign.md` as source of truth.
