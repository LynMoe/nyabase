# Requirements
- Host dashboard must show CPU, memory, disk IO, network IO data.
- Disk usage dimension should display username instead of numeric id.
- Container dashboard must show CPU, memory, disk IO, network IO, not only network IO.
- Image creation should not require/default default user or shell; only UID is needed.
