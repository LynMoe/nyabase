# Worklog
- Started by locating metrics ingestion/query/UI and image form/service/schema paths.
- Restored dashboard HostSection/GpuSection rendering.
- Switched agent container metric emission to Docker stats for CPU, memory, disk IO, and network IO.
- Added backend MetricsWriter identity normalization for numeric user ids and missing container owner labels.
- Removed image defaultUser/defaultShell from schemas, DTOs, entity, UI, tests, and live scripts; exec shell now falls back to /bin/sh.
- Verification: pnpm typecheck; pnpm test:unit; pnpm --filter @nyabase/frontend build; packages/common/src artifact find returned empty.
- Built common/backend/agent/frontend so runtime dist artifacts match source changes.
- Post-build search found no defaultUser/defaultShell references in source, tests, or dist.
- Visual verification: playwright dashboard/images checks passed after updating expected screenshots for intentional HostSection and image-field UI changes.
- Follow-up fix: removed HostSection/GpuSection from dashboard home so user/container dimension tabs are the only chart set on the monitoring screen; server detail still owns the host resource view.
- Follow-up fix: rewrote user/container metric DTO queries to fetch container series without `user_id` filters, map runtime/desired container ids back to owners from DB, normalize full/short runtime ids, and aggregate user charts in backend.
- Follow-up fix: disk usage numeric user ids now resolve through numeric-id lookup plus `findByIds`, so legends return username/displayName instead of numeric project ids.
- Follow-up tests: updated metrics controller unit tests for DB ownership filtering, numeric disk-id name mapping, container resource series, and full/short runtime id normalization.
- Follow-up visual verification: updated dashboard user/container screenshots and confirmed no duplicate host charts; user dimension shows disk usage with display names and container dimension hides disk usage.
- Follow-up verification: pnpm typecheck; pnpm test:unit; pnpm --filter @nyabase/frontend build; pnpm --filter @nyabase/backend build; focused Playwright dashboard/persona checks; packages/common/src artifact find returned empty.
- Deployment follow-up: restarted local backend/frontend with `test/scripts/start-local.sh`; runtime record shows backend pid 3256592 and frontend pid 3256612 started at 2026-06-05T07:56:13.771Z, serving http://localhost:3001/api and http://localhost:5173.
- Deployment follow-up: changed `test/scripts/deploy-agents.sh` to stop only `nyabase-agent`, not `nyabase-docker.service`, so deploying an agent no longer kills running test containers.
- Deployment follow-up: rebuilt/deployed standalone agent; local, CPU remote, and GPU remote `/usr/local/bin/nyabase-agent` all matched sha256 `ec1f1d0a6ea8a2255f0164ec5177bb67ec2d7a80b3167fe0800907faf5a53f02`.
- Runtime fix: Docker stats on the GPU host returned lower-case block IO ops (`read`/`write`), while the agent parser only matched `Read`/`Write`; parser now handles both and unit coverage was updated.
- Runtime evidence: VictoriaMetrics now has `nyabase_container_cpu_usage_ratio`, `nyabase_container_cpu_usage_usec`, `nyabase_container_mem_used_bytes`, `nyabase_container_io_read_bytes_total`, `nyabase_container_io_write_bytes_total`, `nyabase_container_net_rx_bytes_total`, and `nyabase_container_net_tx_bytes_total` for GPU server `a6de7f5d-f4b4-47da-8799-1d25ff03cf94`.
- Runtime evidence: after a controlled write in running container `de84dfda2113`, VM reported `nyabase_container_io_write_bytes_total=462800896`; admin metrics API returned user `admin` and container `test` with nonzero CPU, memory, disk IO, and network IO points.
- UI evidence: Playwright against live 5173 generated `/tmp/nyabase-5173-dashboard-users.png` and `/tmp/nyabase-5173-dashboard-containers.png`; user/container dimension tabs each render an SVG icon and the active icon changes color on tab switch.
- UI evidence: Recharts legend DOM on the live user dashboard had 45 legend texts and `pureNumeric=[]`; disk usage/resource legends now use usernames, not numeric project ids.
- Image schema follow-up: removed image `entrypoint`/`cmd` fields and container desired `entrypoint_json`/`cmd_json` fields from entities, API schemas, DTOs, UI, and tests; SQLite sync dropped those columns in `test/runtime/db/nyabase-test.db`.
- Build hygiene follow-up: backend build now removes `packages/backend/dist` before compiling, which removed stale dist-only migrations that were no longer present in source and still mentioned `entrypoint_json`/`cmd_json`.
- Final runtime restart: reran `test/scripts/start-local.sh`; runtime record shows backend pid 3260529 and frontend pid 3260549 started at 2026-06-05T08:07:49.770Z. Post-restart API quick check showed both agents online, image DTOs without `entrypoint`/`cmd`, and GPU user/container metrics with CPU, memory, disk IO, and network IO points.
- Final verification: `pnpm typecheck`; `pnpm test:unit`; `pnpm --filter @nyabase/backend build`; `pnpm --filter @nyabase/frontend build`; `find packages/common/src \( -name '*.js' -o -name '*.js.map' -o -name '*.d.ts' -o -name '*.d.ts.map' \) -print | sort` returned empty.
- 5173 deployment follow-up: reproduced live 5173 after login selecting `nyabase-test-cpu` from the old dashboard localStorage key, which made the first screen look unchanged because that CPU test host currently has no running container metrics. Updated dashboard server persistence to `nyabase-dashboard-server-v2` and default selection to prefer an online GPU server, then rebuilt frontend and restarted local backend/frontend.
- 5173 deployment evidence: runtime record now shows backend pid 3262963 and frontend pid 3262983 started at 2026-06-05T08:15:08.401Z; `ss` shows node listeners on 3001/5173 and backend logs show both CPU/GPU agents connected after restart.
- 5173 live UI evidence: fresh Playwright context against `http://localhost:5173` logged in as `admin/admin123`, selected `nyabase-test-gpu` by default, rendered user charts with CPU/memory/disk IO/network IO data, disk-usage legends with `pureNumeric=[]`, and container tab with CPU/memory/GPU memory/disk IO/network IO charts; screenshots saved at `/tmp/nyabase-5173-live-users-after-deploy.png` and `/tmp/nyabase-5173-live-containers-after-deploy.png`.
- 5173 source evidence: `curl http://localhost:5173/src/pages/dashboard-page.tsx` contains `nyabase-dashboard-server-v2`, `preferredDashboardServerId`, GPU-priority selection, and active tab icon `text-primary`.
- 5173 API evidence: post-restart API check for `nyabase-test-gpu` returned admin user points/nonzero counts for cpu 19/18, memory 19/19, disk IO 19/7, network IO 23/23; container `test` returned cpu 19/18, memory 19/19, disk IO 19/7, network IO 23/23; image DTOs still have no `entrypoint`/`cmd`.
- 5173 hygiene evidence: frontend `typecheck` and `build` passed; common source artifact check returned empty.
- GPU memory live test: reproduced host `nvidia-smi` showing an external default-Docker `comfyui-nvidia` process consuming ~28GB. Before the fix, agent emitted `nyabase_gpu_proc_mem_used_bytes` for that external Docker container as `container_id=f194...`, causing dashboard container dimension to show an unknown GPU-memory-only container instead of the nyabase `test` container.
- GPU memory fix: agent GPU process metrics now build a nyabase-managed container identity map and only emit GPU process memory for matching managed containers, using the runtime short id as the metric `container_id`; this prevents external Docker GPU processes from polluting nyabase container metrics.
- GPU memory deployment: rebuilt and deployed standalone agent without stopping `nyabase-docker.service`; CPU/GPU agents came online. Local, CPU remote, and GPU remote `/usr/local/bin/nyabase-agent` all matched sha256 `f8b54f5442e17c5a5755bf68f0ac511a80fbc90a0aab215b6b510ad1d0c91e62`.
- GPU memory probe: installed `python3` in the existing GPU `test` container and ran `/tmp/nyabase-gpu-alloc.py`, which uses `libcuda.so.1` through Python `ctypes` to allocate 512MiB and hold it for 90s. Container-local `nvidia-smi` showed the probe PID using ~938MiB on GPU `GPU-8eda9023-68d2-4a5f-be51-e71bfe16ec19`.
- GPU memory VM/API evidence: after clearing stale GPU proc series for the test GPU server and rerunning the probe, VictoriaMetrics returned a single `nyabase_gpu_proc_mem_used_bytes` series for `container_id="de84dfda2113"` with value `983564288`; the backend container metrics API returned only container `test` with `gpuMem.points=2`, `gpuMem.nonzero=2`, `gpuMem.max=983564288`.
- GPU memory frontend evidence: fresh Playwright session on `http://localhost:5173`, server `nyabase-test-gpu`, container dimension, showed `GPU 显存` with no `暂无数据`, legend `test`, and axis values up to `954M`; screenshot saved at `/tmp/nyabase-gpu-container-mem-5173.png`.
- GPU memory verification: `pnpm --filter @nyabase/agent test` passed with 5 files / 76 tests; `pnpm --filter @nyabase/agent typecheck` passed; common source artifact check returned empty.
