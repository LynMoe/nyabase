# Requirements

- Run tests and review the data directory lifecycle for local and remote sources.
- Focus on whether local and remote data directory lifecycle is complete.
- Identify whether lifecycle leaves residual desired state, runtime mounts, or files.
- Actually execute a mount flow.

