# Requirements
- Do not trigger a full state report after every operation; avoid report storms.
- Keep agent full state report cadence at 15 seconds.
- Fix excessive runtime confirmation timeout displays from the root cause.
