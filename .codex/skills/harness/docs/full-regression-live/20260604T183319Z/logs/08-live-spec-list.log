=== live specs ===
--- test/specs/live/dropbear-live-runtime.spec.ts
7:import { describe, expect, it } from 'vitest';
298:describe('live Dropbear user-state runtime on CPU agent', () => {
299:  it('proves non-admin SSH enablement, isolation, repair, key sync, lifecycle, and cleanup', async () => {
461:        cause: describeError(error),
632:  const parts = stdout.trim().split(/\s+/);
1202:      report.cleanup.push(`container cleanup failed ${container.name}:${shortId(container.containerId)} ${describeError(error)}`);
1221:          report.cleanup.push(`exact-prefix residual container cleanup failed ${residual.name}:${shortId(canonicalContainerId(residual))} ${describeError(error)}`);
1226:      report.cleanup.push(`exact-prefix residual scan failed with User A credentials ${describeError(error)}`);
1243:      report.cleanup.push(`SSH key cleanup failed ${key.keyId} ${describeError(error)}`);
1277:    report.cleanup.push(`${label} failed ${describeError(error)}`);
1295:      report.cleanup.push(`temp file removal failed ${basename(path)} ${describeError(error)}`);
1458:    if (regex.test(text)) return label;
1469:  const message = describeError(error);
1470:  if (/Cannot read properties of null \(reading 'state'\)|console kill must confirm Dropbear is not still running|Dropbear kill command (?:left live target processes|did not target a process)/i.test(message)) {
1473:  if (/WebSocket implementation unavailable|ECONNREFUSED|fetch failed|timed out|No route to host|Connection refused/i.test(message)) {
1479:function describeError(error: unknown): string {
1489:    if (/token|secret|password|keyText|authorization/i.test(key)) return '<redacted>';
1573:    for (const line of text.split(/\r?\n/)) {
--- test/specs/live/multi-user-redteam-admin-setup.spec.ts
4:import { describe, expect, it } from 'vitest';
151:describe('multi-user red-team admin fixture setup', () => {
152:  it('creates admin-only live fixtures and non-secret setup manifest', async () => {
331:      .split(/\r?\n/)
--- test/specs/live/multi-user-redteam-alpha.spec.ts
1:import { describe, expect, it } from 'vitest';
5:describe('V2 alpha persona live flow', () => {
6:  it('creates active CPU container, waits actions, exercises stats/power/delete operations, and denies GPU', async () => {
--- test/specs/live/multi-user-redteam-beta.spec.ts
1:import { describe, expect, it } from 'vitest';
5:describe('V2 beta persona live flow', () => {
6:  it('creates beta container and serializes mutations through operation polling', async () => {
--- test/specs/live/multi-user-redteam-continuation.spec.ts
1:import { describe, expect, it } from 'vitest';
5:describe('V2 continuation live flow', () => {
6:  it('continues after setup with operation/action based lifecycle', async () => {
--- test/specs/live/multi-user-redteam-delta.spec.ts
1:import { describe, expect, it } from 'vitest';
5:describe('V2 delta persona live flow', () => {
6:  it('creates CPU and optional GPU containers, then deletes via operation actions', async () => {
--- test/specs/live/multi-user-redteam-epsilon.spec.ts
1:import { describe, expect, it } from 'vitest';
5:describe('V2 epsilon no-access live flow', () => {
6:  it('denies unauthorised container and admin surfaces', async () => {
--- test/specs/live/multi-user-redteam-gamma-delta-attack.spec.ts
1:import { describe, expect, it } from 'vitest';
5:describe('V2 gamma/delta isolation live flow', () => {
6:  it('prevents delta from mutating gamma container by containerId-only route', async () => {
--- test/specs/live/multi-user-redteam-gamma.spec.ts
1:import { describe, expect, it } from 'vitest';
5:describe('V2 gamma GPU persona live flow', () => {
6:  it('creates GPU container when GPU fixture exists and verifies metrics path is non-leaking', async () => {
--- test/specs/live/multi-user-redteam-mount-sources.spec.ts
3:import { describe, expect, it } from 'vitest';
229:describe('multi-user red-team mount sources runtime matrix', () => {
230:  it('verifies user-scoped mount source, data-dir, container mount, dynamic patch, and cleanup behavior', async () => {
326:        cause: describeError(error),
333:      if (alpha) await assertNoResiduals(alpha).catch((error) => recordCleanup('container', alpha, 'residual-check', `failed: ${describeError(error)}`));
334:      if (beta) await assertNoResiduals(beta).catch((error) => recordCleanup('container', beta, 'residual-check', `failed: ${describeError(error)}`));
335:      if (delta) await assertNoResiduals(delta).catch((error) => recordCleanup('container', delta, 'residual-check', `failed: ${describeError(error)}`));
658:      recordCleanup('container', container.actor, container.name, `failed: ${describeError(error)}`);
669:      recordCleanup('data-dir', dir.actor, dir.name, `failed: ${describeError(error)}`);
856:  for (const line of contents.split(/\r?\n/)) {
874:  return Object.fromEntries(Object.entries(body).filter(([key]) => !/refreshToken|accessToken|password|secret/i.test(key)).slice(0, 8));
885:        /token|secret|password/i.test(key) ? '<redacted>' : redact(item),
896:      if (isSensitiveCredentialKey(key) && secret) redacted = redacted.split(secret).join('<redacted>');
912:  if (/Bearer\s+[A-Za-z0-9._-]+/.test(text)) leaks.push('bearer-token-pattern');
913:  if (/"secret"\s*:\s*"[0-9a-f]{64}"/.test(text)) leaks.push('api-secret-pattern');
918:  return /password|token|secret|jwt|refresh/i.test(key);
933:function describeError(error: unknown) {
938:  const message = describeError(error);
939:  if (/fetch failed|ECONNREFUSED|Agent offline|Service Unavailable|websocket/i.test(message)) return 'fail-infra';
--- test/specs/live/ssh-key-fixtures.ts
--- test/specs/live/v2-live-helpers.ts
=== common protocol rest ===
/**
 * REST DTOs and response shapes.
 *
 * Request schemas are defined in `./rest-schema.ts` (Zod, parsed on the
 * server). This file only re-exports the inferred request types and adds
 * response/DTO interfaces that are not validated at runtime.
 */

import {
  UserStatus,
  ServerStatus,
  Capability,
  GpuGrantMode,
  ContainerPhase,
  ContainerPowerIntent,
  ContainerStatus,
  OperationKind,
  OperationStatus,
  HookKind,
  HookStatus,
  RuntimeDriftKind,
} from '../enums.js';
import type {
  ContainerStatsSummary,
  ContainerSshServerStatus,
  DiskInfo,
  GpuInfo,
  DataDirEntry,
  DockerDaemonStatus,
  RemoteFsParams,
  RemoteFsMountStatus,
} from './agent-messages.js';

// Re-export all inferred request types from the Zod schema module so consumers
// can keep importing them from this barrel.
export type {
  LoginRequest,
  RefreshTokenRequest,
  CreateApiTokenRequest,
  CreateUserRequest,
  UpdateUserRequest,
  AddSshKeyRequest,
  CreateServerRequest,
  UpdateServerRequest,
  UpdateServerDefaultsRequest,
  AddDataDiskRequest,
  UpdateDataDiskRequest,
  CreateImageRequest,
  UpdateImageRequest,
  PullImageRequest,
  CreateContainerRequest,
  ExecSessionRequest,
  CreateDataDirRequest,
  CreateGroupRequest,
  UpdateGroupRequest,
  UpsertServerGrantRequest,
  AddGroupMemberRequest,
  AddImageGrantRequest,
  SyncImageGrantServersRequest,
} from './rest-schema.js';

// ---------------------------------------------------------------------------
// Common
// ---------------------------------------------------------------------------

export interface PaginatedResponse<T> {
  items: T[];
  total: number;
  page: number;
  pageSize: number;
}

// ---------------------------------------------------------------------------
// Auth
// ---------------------------------------------------------------------------

export interface LoginResponse {
  accessToken: string;
  refreshToken: string;
  user: UserDto;
}

export interface RefreshTokenResponse {
  accessToken: string;
  refreshToken: string;
}

// ---------------------------------------------------------------------------
// Users
// ---------------------------------------------------------------------------

export interface UserDto {
  id: string;
  username: string;
  displayName: string;
  status: UserStatus;
  createdAt: string;
  /** Resolved union of all group capabilities */
  capabilities: Capability[];
  /** Groups the user belongs to */
  groups: GroupSummaryDto[];
}

export interface SshPublicKeyDto {
  id: string;
  name: string;
  keyText: string;
  createdAt: string;
}

export interface ApiTokenDto {
  id: string;
  name: string;
  lastUsedAt: string | null;
  createdAt: string;
}

export interface CreateApiTokenResponse {
  token: ApiTokenDto;
  /** Raw token, shown only once */
  secret: string;
}

// ---------------------------------------------------------------------------
// Groups
// ---------------------------------------------------------------------------

export interface GroupSummaryDto {
  id: string;
  name: string;
  priority: number;
  isSystem: boolean;
}

export interface GroupDto {
  id: string;
  name: string;
  description: string | null;
  priority: number;
  isSystem: boolean;
  capabilities: Capability[];
  createdAt: string;
  updatedAt: string;
  /** Server IDs that have grants for this group (populated in list endpoint) */
  serverIds?: string[];
  /** Full server grants with resource details (populated in list endpoint) */
  serverGrants?: ServerGrantDto[];
  /** Unique image IDs that have grants for this group (populated in list endpoint) */
  imageIds?: string[];
  /** Members with user details (populated in list endpoint) */
  members?: GroupMemberDto[];
  /** Number of members in this group (populated in list endpoint) */
  memberCount?: number;
}

export interface GroupMemberDto {
  userId: string;
  username: string;
  displayName: string;
}

// ---------------------------------------------------------------------------
// Servers
// ---------------------------------------------------------------------------

export interface ServerDto {
  id: string;
  name: string;
  parentIface: string;
  ipCidr: string;
  gateway: string;
  isGpuServer: boolean;
  status: ServerStatus;
  lastSeenAt: string | null;
  /** Default resource limits applied when a grant leaves fields null */
  defaultCpuMillis: number;
  defaultMemBytes: number;
  defaultDiskBytes: number;
  defaultGpuMode: GpuGrantMode;
  defaultGpuIndices: number[];
  /** Latest persisted/runtime-reported host disk observations */
  disks?: DiskInfo[];
  gpus?: GpuInfo[];
  agentVersion?: string;
  /** Frozen on first agent hello; never changes after that */
  dockerRoot?: string | null;
  dockerSocket?: string | null;
  /** Latest persisted/runtime-reported docker daemon status */
  dockerDaemon?: DockerDaemonStatus | null;
}

export interface ServerAgentTokenResponse {
  /** Raw token, shown only once */
  token: string;
}

// ---------------------------------------------------------------------------
// Data Disks
// ---------------------------------------------------------------------------

/** Mirrors DiskInfo reported by agent; diskId is the stable identifier */
export interface DataDiskDto {
  diskId: string;
  mountPoint: string;
  label?: string;
  totalBytes: number;
  usedBytes: number;
  pquotaEnabled: boolean;
}

// ---------------------------------------------------------------------------
// Images
// ---------------------------------------------------------------------------

export interface ImageDto {
  id: string;
  name: string;
  dockerImage: string;
  defaultUser: string;
  defaultShell: string;
  defaultUid: number;
  description: string | null;
  isActive: boolean;
  /** Override container Entrypoint — space-separated string */
  entrypoint: string | null;
  /** Override container CMD — space-separated string */
  cmd: string | null;
}

// ---------------------------------------------------------------------------
// Grants
// ---------------------------------------------------------------------------

export interface ServerGrantDto {
  id: string;
  scope: 'group' | 'user';
  scopeId: string;
  serverId: string;
  /** null → fall back to server default */
  cpuMillis: number | null;
  memBytes: number | null;
  diskBytes: number | null;
  gpuMode: GpuGrantMode;
  gpuIndices: number[] | null;
  createdAt: string;
  updatedAt: string;
}

export interface ImageGrantDto {
  id: string;
  scope: 'group' | 'user';
  scopeId: string;
  imageId: string;
  serverId: string;
  createdAt: string;
}

/** The resolved access for a user on a specific server */
export interface EffectiveServerAccessDto {
  serverId: string;
