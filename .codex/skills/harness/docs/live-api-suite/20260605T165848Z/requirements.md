# Requirements

- Replace mock/shallow functional testing with real shared-instance API testing.
- Admin must prepare images, storage/data sources, users, groups, quotas, and grants.
- Persona users must operate through real user APIs, preferably separate subagent/persona lanes.
- Common fixtures such as images, data sources, users, groups, and quotas should be persistent/reused.
- Remove old scripts and rewrite the flow.
- Cover all major product functions and run an actual test round.
